# Data-Copilot 自动化工作流

## 完整流程图

```
需求文档/PRD
    ↓
[Phase 1] 需求理解
    ├── 读取设计文档/PRD/需求描述（或引导填写 requirement.md 模板）
    ├── 提取关键信息（数据源、条件、目标表）
    ├── 读取语义模型（knowledge/semantic-model.json 中的口径定义、关联关系和过滤规则）
    ├── 自主识别歧义点并与用户对齐
    ├── 通过 MCP 工具读取源表结构（字段名/类型/分区）
    └── 输出需求理解摘要，用户确认
    ↓ 确认无误
[Phase 2] 设计方案
    ├── 取数逻辑说明（数据来源、过滤条件、关联关系）
    ├── 字段映射关系（源字段 → 目标字段）
    └── 上下游依赖关系
    ↓ 用户确认设计方案
[Phase 3] 表结构设计
    ├── 字段定义（名称、类型、注释）
    ├── 分区设计
    ├── 生成 DDL
    └── [今日增强] 运行 gen_lineage.py 自动生成字段级血缘图
    ↓
[Phase 4] SQL 开发
    ├── 简单场景：子查询 from (select) alias + join (select) on
    ├── 复杂场景：CTE with ... as (...)
    ├── 更复杂：数仓分层设计
    ├── 完成后运行 scripts/validate_sql.py 校验 (含 Smart Fix)
    └── [今日增强] 运行 scripts/estimate_cost.py 进行资源成本预估与风险扫描
    ↓
[Phase 4.5] 代码审查（变更验证，可选）
    ├── 差异比对（线上版本 vs 变更版本）
    ├── 需求覆盖度验证（逐项核对 PRD 变更点是否落地）
    ├── 下游影响分析
    └── 潜在问题检查（重复项、遗漏、映射一致性、逻辑矛盾）
    ↓ 输出审查报告
[Phase 5] 数据质量测试
    ├── [今日增强] 运行 scripts/check_data_quality.py 生成质量仪表盘 (含健康得分与修复建议)
    ├── 记录数校验（非空/唯一）
    ├── 枚举值与映射
    ├── 业务规则反查 (Negative Testing)
    ├── 关联一致性与时效性检查
    └── 波动监控（环比）
    ↓
[Phase 6] 设计文档完善 + 知识沉淀
    ├── 补充完整设计文档（需求、取数逻辑、表结构、数据源、调度、质量、依赖、文件清单）
    ├── 生成项目交付总报告（templates/report.md）
    ├── 语义层模型沉淀（新业务域 → knowledge/semantic-model.json）
    ├── 编码约定沉淀（新规范 → docs/coding-style.md）
    ├── 变量/命名约定沉淀（新约定 → AGENT.md）
    └── [今日增强] 运行 scripts/analyze_productivity.py 更新提效看板
```

## 两种工作模式

| 模式 | 路径 | 触发场景 |
| :--- | :--- | :--- |
| **开发模式** | P1 → P2 → P3 → P4 → P4.5(可选) → P5 → P6 | 从零开发新需求 |
| **审查模式** | P1(需求理解) → P4.5(代码审查) → P5 → P6 | 针对已上线需求进行迭代变更，需验证需求落地情况 |

## 每个阶段的交付物

| 阶段 | 输入 | 输出 |
| :--- | :--- | :--- |
| **Phase 1** | 需求文档 | 需求理解摘要 + 问题清单 |
| **Phase 2** | 确认后的需求 | 设计方案草稿（取数逻辑/字段映射/依赖） |
| **Phase 3** | 确认的设计方案 | **表结构.sql** + 字段级血缘图 |
| **Phase 4** | 表结构 + 数据源 | **核心 ETL SQL** + 校验报告 + 成本预警 |
| **Phase 4.5** | 线上SQL + 变更SQL + 需求 | **{需求名}_审查报告.md** |
| **Phase 5** | 核心SQL + 数据源 | **数据质量测试.sql** + 质量仪表盘 |
| **Phase 6** | 全部成果 | **Design.md + 交付总报告** + 提效看板 |

---
*注：标注为 [今日增强] 的内容为 Data-Copilot 架构升级后引入的自动化引擎能力。*
