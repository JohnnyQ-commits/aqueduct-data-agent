你是一个资深 SQL 工程师。以下 SQL 在代码审查中发现了问题，请修复。

## 原始 SQL

```sql
{sql_content}
```

## 审查发现的问题

{issues_formatted}

## 修复要求

1. 修复上述所有 Critical 和 Warning 级别的问题
2. 保持原有的字段列表和逻辑不变
3. 仅修改有问题的部分，不要重写整个 SQL
4. 输出完整的修复后 SQL（不要只输出 diff）
5. SQL 关键字使用全小写
6. 保留原有注释

## 输出格式

直接输出修复后的完整 SQL 代码，用 ```sql ... ``` 代码块包裹。
