# =============================================================================
# requirements.txt — 自动生成，禁止手动编辑
# =============================================================================
# 生成方式:
#   pip-compile pyproject.toml --output-file=requirements.txt
# 或手动同步 pyproject.toml 的 dependencies + dev dependencies
#
# 唯一依赖声明源: pyproject.toml
# =============================================================================

# Core dependencies
pydantic>=2.0
pydantic-settings>=2.0
anthropic>=0.39
httpx>=0.27

# Dev dependencies
pytest>=9.0.0
pytest-cov>=5.0
ruff>=0.4.0
