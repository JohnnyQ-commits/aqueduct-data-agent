# {需求名称} - 项目交付总报告

> 生成时间：{YYYY-MM-DD}
> 执行模式：{开发模式 / 审查模式}
> 输出目录：`{输出目录路径}`

---

## 一、报告头

- **需求名称**：{需求名称}
- **生成日期**：{YYYY-MM-DD}
- **执行模式**：{开发模式 / 审查模式}
- **目标表**：`{库名}.{表名}`
- **业务域**：{所属业务域名称（如无匹配则标注"新业务域"）}

详细信息参见：

- 设计文档：[Design.md](Design.md)
- 语义层模型：[semantic-model.json](../knowledge/semantic-model.json)（如属于已定义业务域）

---

## 二、源表结构确认

通过 MCP 工具查询并确认的源表清单：

| # | 表名 | 数据库 | 字段数 | 分区字段 | 用途 |
|---|------|--------|--------|----------|------|
| 1 | {表名1} | {库名} | {字段数} | {分区字段} | {用途说明} |

字段名、类型、分区已与需求文档核对完毕，{有/无}差异。

---

## 三、需求理解摘要

### 3.1 取数逻辑

- **数据来源**：{涉及的源表}
- **过滤条件**：{逐条列出}
- **关联关系**：{表间关联方式说明}
- **输出目标**：{目标表及字段}

### 3.2 字段映射

| 目标字段 | 源字段 | 转换逻辑 |
|----------|--------|----------|
| {字段1} | {源字段} | {直接取值 / CASE WHEN / COALESCE 等} |

### 3.3 语义层模型引用

{如属于已定义业务域，列出引用的语义层定义：实体、关系、指标口径、过滤规则等。如为新业务域，标注"本需求为新业务域，后续需生成语义层 JSON 追加到 semantic-model.json"。}

---

## 四、表结构设计

- **表名**：`{库名}.{表名}`
- **分区**：`inc_day` string（格式 `YYYYMMDD`）
- **存储格式**：PARQUET

| 字段名 | 类型 | 注释 |
|--------|------|------|
| {字段1} | {类型} | {注释} |

详见：[表结构.sql](表结构.sql)

---

## 五、核心 SQL 代码

ETL 逻辑：[{需求名称}.sql]({需求名称}.sql)

### 5.1 代码风格确认

- [x] 关键字全小写
- [x] select 字段竖排，4空格缩进
- [x] where 条件紧凑
- [x] 函数内逗号无空格
- [x] join 与 on 分行
- [x] 子查询优先 / CTE 结构

### 5.2 数据流转分析

| 步骤 | CTE/表名 | 输入表 | 转换逻辑 | 输出 | 预估数据量 |
|------|----------|--------|----------|------|-----------|
| 1 | {CTE名} | {源表} | {过滤/转换说明} | {中间结果} | {预估} |

### 5.3 变量使用

| 变量 | 用途 | 示例值 |
|------|------|--------|
| `$[time(yyyyMMdd,-1d)]` | 分区过滤 | `20260528` |
| `inc_day` | 目标表分区 | `20260528` |

---

## 六、代码审查（仅审查模式）

{开发模式跳过本节}

### 6.1 差异比对

| 变更点 | 线上版本 | 变更版本 | 变更类型 |
|--------|----------|----------|----------|
| {行号/逻辑} | {原代码} | {新代码} | {新增/修改/删除} |

### 6.2 需求覆盖度验证

| 需求项 | 是否满足 | 说明 |
|--------|----------|------|
| {需求描述} | {是/否/部分} | {具体说明} |

### 6.3 潜在问题

| 问题类型 | 位置 | 描述 | 修复建议 |
|----------|------|------|----------|
| {重复项/遗漏/映射不一致/逻辑矛盾} | {行号} | {问题描述} | {修复建议} |

详见：[{需求名称}_审查报告.md]({需求名称}_审查报告.md)

---

## 七、数据质量测试

测试用例：[数据质量测试.sql](数据质量测试.sql)

### 7.1 测试用例清单

| # | 测试类别 | 测试内容 | 预期结果 | 实际结果 |
|---|----------|----------|----------|----------|
| 1 | 记录数校验 | 结果表不为空 | > 0 | {待执行} |
| 2 | 记录数校验 | 主键不重复 | 0 条 | {待执行} |
| 3 | 枚举值校验 | 字段合法 | 仅预期枚举值 | {待执行} |
| 4 | 关联反查 | 结果表数据与源表一致 | 0 条差异 | {待执行} |
| 5 | 字段非空 | 关键字段非空 | 全部为 0 | {待执行} |
| 6 | 波动监控 | 环比波动 < 50% | 正常范围 | {待执行} |
| 7 | 边界用例 | {具体场景} | {预期} | {待执行} |

### 7.2 测试覆盖度

- 总用例数：{N}
- 通过：{N}
- 失败：{N}
- 待执行：{N}

---

## 八、上下游依赖

### 8.1 上游依赖

| 依赖表/任务 | 分区条件 | 产出时间 | 依赖类型 |
|-------------|----------|----------|----------|
| {上游表1} | inc_day = T-1 | {时间} | 强依赖 |

### 8.2 下游依赖

| 下游表/报表/任务 | 依赖方式 | 影响说明 |
|-------------------|----------|----------|
| {下游应用} | 读取目标表 | {影响说明} |

### 8.3 调度配置

- **调度频率**：每天一次（T+1）
- **调度时间**：上游表产出后执行
- **分区变量**：`inc_day = $[time(yyyyMMdd,-1d)]`
- **失败策略**：告警通知 + 重试

---

## 九、知识沉淀

| 沉淀类型 | 内容 | 目标文件 | 状态 |
|----------|------|----------|------|
| {语义层模型} | {新业务域定义} | knowledge/semantic-model.json | {待确认/已追加} |
| {编码约定} | {新规范描述} | CONTRIBUTING.md | {待确认/已追加} |
| {命名约定} | {新约定描述} | CONTRIBUTING.md | {待确认/已追加} |

---

## 十、交付物清单

| 文件 | 用途 | 状态 |
|------|------|------|
| 表结构.sql | 目标表 DDL 定义 | {已完成} |
| {需求名称}.sql | 核心 ETL 逻辑 | {已完成} |
| 数据质量测试.sql | DQC 测试用例 | {已完成} |
| Design.md | 完整设计文档 | {已完成} |
| {需求名称}_审查报告.md | 代码审查报告 | {已完成/不适用} |
| 本总报告.md | 项目交付总报告 | {已完成} |

---

## 十一、执行检查清单

- [ ] 确认分区变量 `$[time(yyyyMMdd,-1d)]` 已正确解析
- [ ] 确认上游表在目标分区有数据产出
- [ ] 确认目标库有写权限
- [ ] 确认 ETL 代码已通过数据质量测试
- [ ] 确认语义层模型已更新（如为新业务域）
- [ ] 确认下游任务调度依赖已配置

---

## 附录

### 语义层模型摘要（如适用）

{如本需求属于已定义业务域，附上该业务域的语义层 JSON 摘要：实体清单、关系清单、指标口径。}

### 代码风格规范引用

本项目的 SQL 代码遵循 `CONTRIBUTING.md` 中定义的规范。
